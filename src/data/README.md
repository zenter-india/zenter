# src/data

TanStack Query hooks + query-key definitions (AD-2) and realtime subscription wiring (AD-5) built on top of `src/api`. Canonical query keys (`['feed',…]`, `['connections',userId]`, `['messages',conversationId]`, `['config']`, `['profile',phone]`, `['status',phone]`, …) are defined here in Story 1.4. Empty in Story 1.1.
