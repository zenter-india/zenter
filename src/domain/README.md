# src/domain

Pure domain logic with no I/O: matching/gating rules (AD-9), the relationship state machine (ported from web `js/relationships.js`, AD-6), contact-masking (AD-8), and the bundled location dataset (states/districts/CMS centres). Unit-testable in isolation. Empty in Story 1.1.
