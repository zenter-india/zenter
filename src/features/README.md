# src/features

Feature modules (one folder per feature area: auth, onboarding, feed, connections, chat, contact-exchange, profile, safety). Each feature composes screens/components and consumes `src/api`, `src/stores`, `src/domain`. Populated from Epic 2 onward. Empty in Story 1.1 (scaffold).
