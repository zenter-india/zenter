# src/stores

Zustand client-state stores (AD-2): ephemeral UI/session state only — NOT server data (server data is TanStack Query, added Story 1.4). The relationship projection (AD-6) and device-local read-state (AD-11) land here in later stories. Empty in Story 1.1.
